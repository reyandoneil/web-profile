import React from 'react'
import {ButtonWraper} from './ElementsGeneralButton'

function GeneralButton({text}) {
    return (
        <ButtonWraper>
            {text}
        </ButtonWraper>
    )
}

export default GeneralButton
